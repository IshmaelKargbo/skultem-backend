package com.moriba.skultem.domain.vo;

// Whether a pay component (an allowance or deduction line, see PayComponent) is a flat amount or a
// percentage of the basic salary it's attached to.
public enum PayComponentType {
    FIXED, PERCENTAGE
}
