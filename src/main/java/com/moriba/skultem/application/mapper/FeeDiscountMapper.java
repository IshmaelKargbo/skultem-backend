package com.moriba.skultem.application.mapper;

import com.moriba.skultem.application.dto.EnrollmentDTO;
import com.moriba.skultem.application.dto.FeeDiscountDTO;
import com.moriba.skultem.domain.model.FeeDiscount;

public class FeeDiscountMapper {
    // public static FeeDiscountDTO toDTO(FeeDiscount param) {
    //     if (param == null) return null;
        
    //     EnrollmentDTO enrollment = EnrollmentMapper.toDTO(param.getEnrollment());

    //     return new FeeDiscountDTO(param.getId(), param.getName(), param.getKind(), enrollment, param.getValue(),
    //             param.getExpiryDate(), param.getReason(), param.getCreatedAt(), param.getUpdatedAt());
    // }
}
